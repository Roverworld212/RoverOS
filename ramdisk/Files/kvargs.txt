{
    SYNTAX
    {} Defines a comment, they will be ignored and will not be added to the ramdisk
    ; defines end of current statment
    () defines arg
    <> defines a no parse statment, anything between will not be parsed (Must be in a comment)
    <#> defines terminator, all data past the terminator will be ignored and will not be written to the ramdisk
    Examples:
    {My statment <Don't Parse#>}
    -my-statment(my-args);
    myStatment(MyArgs);
    my statment(my args);
    myStatment(my)(args);
}
{Change the kernel name}
-set-kernel-name(Anza); {This statment has some issues}
{Change Log Values}
-set-log(1);
-set-debug-log(1);
-set-info-log(1);
-set-warn-log(1);
-set-error-log(1);
-set-panic-log(1);
-set-kmalloc-log(0);
-set-malloc-log(0);
-set-clear-set-log(0);
-set-print-frames(0);
{Setup UI elements}
{Should draw text}
-set-draw(1);
{Enable V-Sync (Not added yet)}
-set-vsync(0);
{Buffering mode}
-set-buffer-mode(0);
{X start pos after newline}
-set-x-offset(2);
{Start pos}
-set-lpx(2); 
-set-lpy(2);
{Space between chars}
-set-text-padding(7);
{Y pixels to skip on newline}
-set-newline-padding(4);
{Font Size in PX}
-set-font-x-size(7);
-set-font-y-size(7);
{Should the text be scaled?}
-set-scale(0);
{Y Scale}
-set-y-scale(1);
{X Scale}
-set-x-scale(0);
{Draw the 0 bit in font (Drawn as bg color)}
-set-draw-back-color(true);
#